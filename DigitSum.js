let numbers = prompt("Enter Multiple numbers: ");
numbers = numbers;
let sum = 0;
for (let i = 0; i < numbers.length; i++){
  sum += parseInt(numbers[i]);
}
document.write("The sum of the NUmber is: " + sum);



// for lopp Multiple input Digit Sum in JavaScript
